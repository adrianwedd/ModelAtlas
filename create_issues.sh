gh issue create --title "🗑️ Cleanup debug HTML files" --body "Remove temporary and debug HTML files as outlined in the cleanup plan. Files: `debug_deepseek-r1.html`, `debug_deepseek-r1_tags.html`, `temp_ollama_page.html`, `ollama_debug_dumps/llama2_tags_page.html`" --label "area:cleanup,status:todo,priority:high"
gh issue create --title "🗑️ Remove debug directory" --body "Remove the entire `ollama_debug_dumps/` directory." --label "area:cleanup,status:todo,priority:high"
gh issue create --title "🧹 Clean up log files" --body "Decide on a retention policy for `.log` files and clean them up. Options: delete all, archive, or add to `.gitignore` and remove from tracking." --label "area:cleanup,status:todo,priority:high"
gh issue create --title "🤔 Review unused scripts" --body "Review potentially unused scripts and decide whether to remove them: `similarity_engine.py`, `generate_readme.py`, `generate_visuals.py`" --label "area:cleanup,status:todo,priority:medium"
gh issue create --title "🗑️ Remove empty directory" --body "Remove the empty `codex/` directory." --label "area:cleanup,status:todo,priority:medium"
gh issue create --title "🗑️ Remove one-time migration script" --body "Remove the `migrate_tasks_to_issues.py` script now that the migration is complete." --label "area:cleanup,status:todo,priority:medium"
gh issue create --title "📚 Consolidate prompt files" --body "Review and consolidate the various prompt-related markdown files: `LLM_ENRICHMENT_PROMPTS.md`, `LLM_PROMPTS.md`, `enhanced_enrichment_prompts.md`." --label "area:documentation,status:todo,priority:medium"
gh issue create --title "🔍 Audit root-level data files" --body "Audit root-level data files (`models_enriched.json`, `models_raw.json`) to check for duplication with data in `enriched_outputs/` and `data/`." --label "area:cleanup,status:todo,priority:medium"
gh issue create --title "🧹 Update .gitignore" --body "Add common cleanup patterns and temporary file locations to `.gitignore`: `*.log`, `*debug*.html`, `temp_*.html`, `**debug**`, `ollama_debug_dumps/`" --label "area:repo,status:todo,priority:medium"
gh issue create --title "🗄️ Verify large files are in LFS" --body "Verify that large JSON files (>100MB) are correctly tracked via Git LFS, especially in `enriched_outputs/` and `data/`." --label "area:repo,status:todo,priority:medium"